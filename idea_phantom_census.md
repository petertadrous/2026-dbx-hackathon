# Idea: Phantom Census

**Track:** 2 Medical Desert (with a Track 1 twist)
**One-liner:** Subtract statistically nonexistent facilities first — every desert map is wrong until you do.

> **Day-0 Validation Update (2026-06-15).** Full validation suite (`phantom_census_validation_results.md`) and schema audit (`phantom_census_schema_audit.md`) executed against the live Databricks Marketplace share. Both kill-switches cleared (geocoding 98.8%; PIN-vs-spatial 24.5% disagreement after light normalization). Three locked-in changes vs. the original draft below: (1) **demo state pivots Bihar → Maharashtra**, Bihar yields only 24 single-flag phantoms (fails T3.2 ≥50); Maharashtra yields 304 phantoms across 33 districts with BEED rank 10→2 as the headline shuffle. (2) **Uniqueness test moves off `description` onto `capability`+`procedure`+`equipment`** — description p50 is 16 tokens, MinHash@Jaccard 0.9 + shingle-5 won't work; the structured-claim arrays are 99.7% populated and far richer. (3) **NFHS-consistency test re-spec'd from "indicator didn't move" → "claims capability X in a bottom-quartile NFHS district for X"** — NFHS-5 is a snapshot, "didn't move" requires NFHS-6 longitudinal which is out of scope. Two additional non-blocking gotchas the implementation must respect: `address_stateOrRegion` is contaminated (254 distinct values vs. 36 real states — derive from coordinates) and `source_content_id` is a per-source-page ID (NOT an entity key — 27 different hospitals can share one).

---

## Snapshot

- **Estimated MVP effort:** ☐ S (≤20h) ☐ M (20–35h) ☑ **L (35–55h)** ☐ XL (>55h) — ~48h + 6h buffer across 3 engineers. Base ~30.5h plus +12h for AI-centrality patch (embedding-drift to MVP, Foundation Model API for contested cases, IE agent runtime path, pydeck migration) plus +5–6h for user-value views (Budget Reallocation, Audit Queue, Genie sidebar). Cut path defined in §3 if hours run short.
- **Stretch features beyond MVP:** S3 watch prefix dropping CKAN-compatible CSV when district scores cross threshold; mock state HMIS webhook receiving structured priority deltas; pluggable existence-test enum (add tests as new rows, not code); planner-override veto rule with required reason; secondary indicator overlay (e.g. NFHS institutional-delivery rate) on hover. **Cross-pollinated differentiators (see Section 5):** bitemporal time-slider replay (rewind desert map; prove we'd have flagged Nalanda months before the official survey); gold-set live precision/recall model card (80 hand-labeled facilities, F1 shown live in dashboard); cross-track cascade preview (phantom subtraction → ASHA referral corridors re-route in same frame, Track 2 demo proves Track 3 value); silent-degradation 6th existence test (description-embedding cosine drift via BYTEA pgvector); leverage-weighted phantom queue ordering (sort overrides by NFHS-5 mortality-burden × population, not alphabetical).
- **Rubric score (out of 25):** *Filled by AI applying strict 1pt/3pt/5pt anchors per `Hackathon Topic Prioritization Rubric.md`; team should re-score.* Decoupling **5/5** (Existence Prosecutor + Existence Defender + deterministic Adjudicator + Information Extraction agent — matches 5pt anchor "complex network of parallel cooperating specialized agents") · Integration **3/5** (S3 watched prefix is a real write; HMIS webhook is *mocked*, not a real state system — 3pt anchor "standard REST APIs but writes require custom workarounds" is the strict read) · Testability **4/5** (PIN reverse-lookup, MinHash Jaccard threshold, spatial join consistency, NFHS bottom-quartile, temporal implausibility are all binary/deterministic; embedding-drift is a continuous cosine score with a threshold — honest down-rate from 5; Adjudicator is still majority-with-veto, no LLM at the verdict layer) · Pain **5/5** (misallocated PMJAY-empanelment funding to phantom-inflated districts is a massive financial leak with direct ROI metric — 5pt anchor) · Tokens **4/5** (existence-test math and adjudication remain non-LLM; LLM is activation-gated to ~3% contested cases via Foundation Model API for Defender corroboration synthesis and Adjudicator-contested escalation; honest down-rate from 5 because the *output* is generated, but inputs are pre-structured and gating keeps full-scan cost ≤ $1) · **Total 21/25 — green light per rubric (22–25 band) sits one point above; team should pressure-test Testability (some judges may keep at 5 since the threshold is binary even if the underlying score is continuous) and Integration (a real CKAN endpoint moves Integration to 4 and total to 22).**
- **Rubric calibration note:** Two single-point drops vs. the original 23/25 reflect the AI-centrality patch — embedding-drift is continuous (Testability 5→4) and FMA is now load-bearing on contested cases (Tokens 5→4). The trade is a complete answer to *"where's the AI?"* in exchange for two rubric points; the team judged the AI story more important than the tie-breaker. Integration loses two points because the HMIS webhook is mocked; if the team can wire a real CKAN endpoint (S3 prefix already counts), Integration moves to 4 and total to 22 — back inside the green band.

---

## 1. High-Level Design

- **User & moment of use:** A state planning commissioner deciding where to place the next 50 PMJAY-empanelled facilities for the quarter. Has a budget, a map, a deadline; cannot afford to ignore a truly underserved district while funding one with inflated coverage.
- **Core question the app answers:** *Which districts are real medical deserts once we subtract facilities that fail multiple independent existence tests?*
- **Primary workflow (≤7 steps):**
  1. Pick clinical capability (e.g. maternity); view the official ("raw") Maharashtra desert choropleth with all facilities rendered as ✅ green / 👻 grey-ghost / ⚠️ yellow dots based on existence verdict.
  2. Toggle **include phantoms in score** off → choropleth re-colors using phantom-adjusted desert score; ghosts stay visible on the map (the dataset's claims are still shown — they just stop counting toward "coverage"); ranking shuffles; counter shows "304 phantoms removed across 33 districts."
  3. Click a now-redder district (e.g. BEED) → side panel with example phantoms ranked by leverage (`mortality_burden × population × phantom_density`), each rendered with failed existence tests + clickable evidence chips back to the source row (India Post PIN, MinHash cluster, NFHS-5 indicator).
  4. Click a `contested` phantom → AI panel surfaces the Defender's Information-Extraction quotes from district bulletins/RTI replies and the Foundation Model API's recommendation with reasoning ("evidence weak; recommend force-phantom"). Planner overrides force-real or force-phantom with a required reason note; district score recomputes live; saved to Lakebase.
  5. Switch to **Budget Reallocation** view: side-by-side before/after pie chart of the planner's quarterly allocation; recommended ₹ shifts from phantom-inflated districts to true deserts; one-click `Export revised allocation CSV`.
  6. Switch to **Audit Queue** view: leverage-weighted top-50 inspector worksheet (PIN, GPS, three failed tests as a pre-printed checklist); one-click `Export inspector worksheet PDF`. Override one entry live to show the queue rebuilds.
  7. Click **export plan**: CKAN-compatible CSV lands in a watched S3 prefix; mock state HMIS webhook fires with the priority delta. Save scenario in Lakebase; reload survives.
- **Inputs:** VF facility records (rich `capability` / `procedure` / `equipment` JSON-array fields are 99.7% populated and used as the primary claim text; `description` is supporting context only — p50 = 16 tokens, too short for the uniqueness test); India Post PIN directory (165k rows; cardinality-aware: PIN→post-office is one-to-many); NFHS-5 district indicators (706 × 109, already snake_cased + numeric in the marketplace share); geoBoundaries India ADM2 shapefile (735 polygons, downloaded — geoboundaries.org direct is sandbox-blocked, use GitHub LFS media endpoint) for spatial join; planner overrides; threshold configs (Jaccard, NFHS bottom-quartile cutoff).
- **Fields explicitly NOT trusted as input:** `address_stateOrRegion` (contaminated — 254 distinct values vs. 36 real states; contains JSON fragments and city names; state is derived from coordinates via spatial join instead); `source_content_id` (per-source-page ID, NOT entity-level — used only as a citation back-reference, never as a join key or facility identifier).
- **Outputs:** UI = pydeck choropleth + facility scatter (ghosts visible) + phantom side panel + Budget Reallocation view + Audit Queue view + Genie NL chat sidebar; Lakebase = `phantom_verdicts`, `desert_scores` (raw and adjusted, with delta), `team.planner_overrides`, `team.saved_scenarios`, `cache.description_embeddings` (pgvector); CKAN-compatible **CSV in S3** (planner export) + structured webhook payload to mock HMIS; Budget Reallocation **revised-allocation CSV** (which districts gain/lose ₹ vs. the dirty-map allocation); Audit Queue **inspector worksheet PDF** (top-50 leverage-weighted facilities with PIN/GPS/failed-test checklist); planner override audit trail.
- **Out of scope:** Live web validation of facility addresses; supply-elasticity / building-decision modeling; non-spatial deserts; resurrecting flagged phantoms via NLP; predicting *future* phantom risk; routing / referral logic; modifying source datasets in place; fixing wrong NFHS-5 district names via string matching (spatial join only).

---

## 2. Databricks Technologies & Centrality

### Lakebase
- Used for: per-district incremental recompute of `desert_scores` triggered by `phantom_verdicts` writes; mutable state for verdicts, planner overrides, saved scenarios; pre-computed MinHash signatures stored as BYTEA; serves the live UI redraw without re-batching the full 706-district aggregate per click.
- Specific tables / indexes / pgvector use: `operational.facility_existence_tests` (composite PK on `(facility_id, test_name, ran_at)`, liquid-clustered on `facility_id` in Delta mirror), `operational.phantom_verdicts` (CDC source for map redraw), `operational.desert_scores` (materialized view via trigger), `operational.facility_evidence` (IE-extracted corroboration rows feeding the Defender), `cache.description_minhash` (BYTEA), **`cache.description_embeddings` (BYTEA, pgvector cosine index — promoted to MVP for the 6th existence test, embedding-drift; loaded into numpy at app start, ~30 MB at 384 dims for 10k facilities)**, `team.planner_overrides`, `team.saved_scenarios`, `team.budget_allocations` (planner's prior-quarter allocation for the before/after reallocation view). **Stretch:** bitemporal columns `(valid_from, valid_to)` on `facility_existence_tests` and `desert_scores` to power the time-slider replay (cross-pollinated from Counterfactual Witness).
- Centrality: ☑ **Load-bearing** ☐ Supporting ☐ Nice-to-have
- Removal cost: The toggle's visible-redraw effect *is* per-district incremental recompute on CDC. With Delta-only you'd re-batch the full 706-district aggregate per click; the toggle freezes and the demo's "shuffle the ranking live" beat collapses. Override + scenario persistence also requires multi-writer mutable state. Removing Lakebase costs the *demo*, not just a feature.

### Agent Bricks
- Agent types used: ☑ Information Extraction ☐ Knowledge Assistant ☑ Multi-Agent Supervisor ☐ Custom LLM
- Specific agent roles & responsibilities:
  - **Existence Prosecutor** — runs the six existence tests (PIN reverse-lookup, MinHash on `capability`/`procedure`/`equipment`, spatial-join consistency, NFHS bottom-quartile snapshot, temporal implausibility, **embedding-drift cosine** — the 6th, promoted to MVP); defaults "fake until proven real"; emits failed-test rows.
  - **Existence Defender** — looks for corroborating signals to **rescue** accused phantoms. Owns two layers: (a) **Information Extraction (Agent Bricks IE)** at runtime — mines VF descriptions, district health bulletins, NFHS report annexes, RTI replies for facility mentions; output is structured `{source_type, source_url, mention_text, mention_confidence}` rows in `operational.facility_evidence`. (b) **Dataset-version reconciliation** — a PIN-vs-spatial disagreement caused by post-2022 district reorganization (Bapatla carved from Prakasam, NTR from Krishna) or spelling drift (Mysore↔Mysuru, Ahmadnagar↔Ahmednagar) must be rescued, not flagged. Day-0 validation showed ~9pp of the 24.5% raw PIN-vs-spatial disagreement is in this bucket.
  - **Adjudicator** — deterministic majority-with-veto rule (PIN failure = hard veto; claim-uniqueness alone is not). **No LLM at the verdict layer.**
  - **Foundation Model API (`ai_query`)** — load-bearing for two activation-gated paths: (1) **Defender corroboration synthesis**: given 3–5 IE-extracted evidence rows of varying quality, output structured `{strength: weak|medium|strong, supporting_rows, reasoning}`. (2) **Adjudicator-contested escalation**: when majority-with-veto outputs `contested`, the LLM reads all evidence rows + the planner's prior override notes for similar facilities and emits an advisory recommendation with reasoning — the planner still clicks force-real / force-phantom. Activation gate fires on the ~3% contested verdicts only, keeping full-scan cost ≤ $1. Models: `databricks-meta-llama-3-1-70b-instruct` primary, `databricks-mixtral-8x7b-instruct` fallback. Template-first generation as a fallback path so the pipeline cannot fail on LLM unavailability.
- Centrality: ☑ **Load-bearing** ☐ Supporting ☐ Nice-to-have
- Removal cost: Without IE at runtime, the Defender has only structural-claim arrays to work with — the contested-case rescue rate drops materially and the demo's "AI advised, math decided, human overrode" beat collapses to "math decided, human overrode." Without FMA on contested cases, contested verdicts are an unreasoned shrug instead of an explained recommendation. The deterministic Adjudicator keeps working without either, but the AI-centrality answer to *"where's the AI?"* disappears.

**Framing for judges:** The verdict adjudication rule is deterministic by design — auditability requirement, not a posture. AI is load-bearing in four places where determinism fails: free-text Information Extraction for corroborating evidence, embedding-cosine drift for silent-emergence detection, Defender corroboration synthesis on contested cases, and Adjudicator escalation for human-in-the-loop. LLM activates on ~3% of facilities — the contested ones — keeping cost ≤ $1 per full national scan while doing work only LLMs can do. **Determinism owns the *math*; AI owns the *evidence*; the human owns the *decision*.**

### Databricks Apps
- **Frontend stack: Streamlit + pydeck (deck.gl)** — locked, not deferred. GPU-rendered (handles 706-district choropleth + 10k facility scatter without lag), layer-composable (toggle is a data-prop swap on GeoJsonLayer, not a re-render), first-class click handlers via `st.session_state`, deploys on Databricks Apps with no extra dependencies. Folium rejected because Leaflet-DOM hits performance walls at 10k facility points and click-into-region requires custom JS. Gradio rejected because it has no first-class map component. React + FastAPI rejected because the ~10–15h state-model setup overhead doesn't fit the L bucket.
- **Layer composition:**
  - `GeoJsonLayer` — 706 ADM2 districts, fill = desert score (raw OR phantom-adjusted per toggle); pickable, auto-highlight.
  - `ScatterplotLayer` — 10k facilities, color by verdict (✅ green / 👻 grey ghost / ⚠️ yellow contested); **filter is NOT applied on toggle — phantoms remain visible as ghosts even in the adjusted view, so judges see the lie the dataset was telling rather than facilities silently disappearing.** The toggle changes choropleth fill, not facility visibility.
  - *(stretch)* `ArcLayer` — ASHA referral corridors, animated re-route on toggle.
- **Three Streamlit views (tabs in one app):**
  1. **Map view** (the wow toggle) — pydeck choropleth + ghost scatter; phantom side panel with evidence chips and the AI contested-recommendation block.
  2. **Budget Reallocation view** — before/after pie chart of the planner's quarterly allocation, recommended ₹ shifts table sorted by mortality-burden, one-click `Export revised allocation CSV`. This is the *"so where does the money actually go?"* deliverable, not a stat.
  3. **Audit Queue view** — leverage-weighted top-50 inspector worksheet (PIN, GPS, three failed tests as pre-printed checklist), one-click `Export inspector worksheet PDF`. This is the *"who do my inspectors visit Monday?"* deliverable. Override one entry live → queue rebuilds.
- **Genie sidebar** — always-visible NL chat over `desert_scores`, `phantom_verdicts`, `team.budget_allocations`. Lets a non-technical commissioner ask ad-hoc questions ("which 5 districts gain the most rank from subtraction?", "show me budget shifts > ₹5 Cr") that no pre-baked view will cover. Genie writes SQL against Lakebase + Delta and renders a chart inline. Doesn't touch the verdict path so the deterministic-core claim survives.
- **How it reads/writes Lakebase live:** Subscribes to `desert_scores` for the choropleth; subscribes to `phantom_verdicts` + `facility_existence_tests` + `facility_evidence` for the side panel; writes planner overrides to `team.planner_overrides` and saved scenarios; export triggers fire S3 + webhook hooks; reads `team.budget_allocations` for the reallocation before/after.
- **Auth / multi-user considerations:** Single planner persona for the demo; scenario name is user-provided so multiple saves co-exist; multi-planner concurrency on the same district is a stretch goal.
- **Day-1 spike (1h, blocking):** deploy a hello-world pydeck app to Databricks Apps with one GeoJsonLayer (706 polygons) + one ScatterplotLayer (10k points). Verify render, click handler, layer toggle. Fallback if pydeck unavailable: Plotly `choropleth_mapbox` + `scattermapbox` stacked traces (loses ArcLayer animation; everything else works).

### Other Databricks tech (optional)
- **Geospatial functions** (`ST_Contains`, `ST_Point`) load-bearing for the NFHS-5 spatial join (string-matching district names is unreliable). *Marker to validate on Free Edition; fallback to GeoPandas in-process if unavailable* (10k facilities × 700 polygons fits a single worker). Delta mirror of Lakebase tables for analytical scans. **Liquid clustering on `facility_id`** in Delta mirror. MLflow `@mlflow.trace` decorators on each existence test / agent run.
- **Genie / Genie Spaces** — supporting; powers the always-visible NL chat sidebar over `desert_scores`, `phantom_verdicts`, `team.budget_allocations`. The one place an LLM owns the user-facing experience without touching the verdict path. Memorable judge moment for ad-hoc questions ("which districts gain the most rank?") that the pre-baked views don't cover.
- **Vector Search** — supporting; powers the *"what 14 facilities are in this MinHash cluster?"* drill-down on the side panel (embedding cosine ranks the cluster) and is the storage substrate the embedding-drift 6th existence test reads at app start. Does not gate the verdict (MinHash + drift run independently).

---

## 3. Presentation & Wow Factor

### 3-minute demo arc
| Time | What's on screen | What the presenter says |
|------|------------------|-------------------------|
| 0:00–0:20 | Maharashtra choropleth, official desert score by district. 10k facilities rendered as ✅ green / 👻 grey ghost / ⚠️ yellow dots. | "Every desert map you've seen is wrong. Most maps add up the facilities they can find. We've already tested whether each one actually exists — the grey ghosts are the ones that don't. Today's planners fund the loud districts; we're going to fund the sick ones." |
| 0:20–0:50 | Toggle **include phantoms in score** off → choropleth re-colors using phantom-adjusted score; ghosts remain visible on the map; ranking visibly shuffles; counter ticks "304 phantoms removed across 33 districts." BEED moves 10 → 2. | "One toggle. Six existence tests fired against ten thousand records. Notice the ghosts didn't disappear — the dataset still claims they exist. We just stop counting them as coverage. The ranking shuffles. BEED was tenth; it's actually the second-worst desert in the state." |
| 0:50–1:30 | Click BEED → side panel with three example phantoms ranked by leverage; each shows failed-test grid with clickable evidence chips → expand to show India Post PIN row, MinHash cluster of 14 near-identical capability arrays, NFHS-5 indicator value with bottom-quartile cutoff line. | "Phantom A: PIN says Aurangabad, GPS says Solapur — fails India Post reverse-lookup, click for the row. Phantom B: capability claims are a 0.97-Jaccard near-duplicate of 14 other facilities. Phantom C: claims maternity in a district where NFHS-5 institutional-delivery sits in the bottom quartile. Three deterministic tests; every claim cites a row." |
| 1:30–2:00 | Click a **contested** Phantom D → AI panel opens showing Defender's Information-Extraction quotes from a 2024 district health bulletin and the Foundation Model API's structured recommendation: *"evidence weak; recommend force-phantom; reasoning: PIN mismatch survives, no corroborating mention in 2024 bulletin, embedding cosine 0.71 from baseline."* Planner disagrees, overrides force-real with their own note. District score recomputes live. | "Contested doesn't mean shrug. Information Extraction pulled three weak corroborations from the bulletin; the model weighed them and recommended phantom — with reasoning. I disagree on this one. I override force-real, with my own note. AI advised, the math decided, I overrode. Every step has a row in Lakebase." |
| 2:00–2:30 | Switch to **Budget Reallocation** tab: side-by-side before/after pie of the planner's ₹50 Cr Q3 allocation; recommended ₹6 Cr shifts toward BEED, Latur, Osmanabad. Click `Export revised allocation CSV`. | "The map changing means the *money* changes. Old allocation sent ₹8 Cr to a district ranked tenth that's actually second. Recommended re-allocation: shift ₹6 Cr toward the real deserts. Export the revised plan." |
| 2:30–2:50 | Switch to **Audit Queue** tab: leverage-weighted top-50 inspector worksheet (PIN, GPS, three failed tests as a checklist). Click `Export inspector worksheet PDF`. Override one entry live; queue rebuilds. | "Fifty inspectors, fifty visits this quarter. Leverage-weighted by mortality burden × population × phantom density. Print the worksheet — every row has the PIN, the GPS, and the three tests an inspector should verify on the ground." |
| 2:50–3:00 | Click **export plan**: CKAN-compatible CSV → S3, mock HMIS webhook fires. Save scenario "Q3 Maharashtra Maternity Audit"; sidebar shows token usage (gated to ~3% of records), MLflow trace links, gold-set F1. Differentiation hook on screen. | "CSV in S3. HMIS webhook fired. Scenario saved. Token usage stays gated to the contested cases. Determinism owns the math; AI owns the evidence; the planner owns the decision. Every other desert map will *add* facilities to find gaps. We *subtract* fictional ones to expose the deserts hiding in plain sight." |

### The wow moment
**The toggle, the ghosts, and the cascade.** ~10 seconds: planner clicks `include phantoms in score` off, choropleth re-colors, BEED goes from beige to red, ranking shuffles in the sidebar — but the 👻 ghost dots stay on the map. The dataset's claims are still visible; they just stop counting toward "coverage." That single visual frame conveys the whole thesis: *the lie isn't gone, we're just no longer believing it.* The cascade follows on the override beat — facility tile flips → district score recomputes → state ranking re-sorts → audit queue rebuilds — all driven by one CDC subscription. The visual tells the entire thesis without a sentence of narration.

### Alternative wow beats (stretch — pick one if a stretch ships)
- **Bitemporal rewind (Counterfactual Witness origin):** Drag the time slider to "March 2024" — the choropleth re-renders in the historical world-state. Nalanda was *already* phantom-inflated then; the official survey didn't catch it until December. Single sentence: "We would have flagged this nine months early." Audit receipt cites public-event timestamps so the comparison is falsifiable.
- **Cross-track cascade (Regional Cascade + Care Pathway origin):** After the phantom subtraction, a side overlay shows ASHA referral corridors *re-routing* in real time — 12 maternity referrals that previously assumed Phantom Hospital A is real now flow to verified Facility B (with extra distance flagged). One Track 2 demo proves Track 3 value without leaving the app.
- **Live model card (TrustLens / ClaimsCleanr origin):** Bottom-right panel ticks "80-record gold set: 87% precision @ 0.9 conf, 0.81 F1." When the planner overrides Phantom A back to real, the panel recomputes. Judges see auditable accuracy, not vibes.

### How the demo proves the four required capabilities
- **Strict citations:** Each phantom verdict in the side panel cites the exact India Post row (PIN, lat/long), the MinHash collision set (with duplicate facility IDs), and the NFHS-5 indicator value with district code — all clickable. No claim without a row reference.
- **Uncertainty mitigation:** Three test outcomes are independent → verdict is `phantom | real | contested`; "contested" is a first-class verdict explicitly flagged in the side panel; one-test failures don't trigger phantom status (Adjudicator's veto-aware rule); side panel labels which test acted as veto vs supporting. Records with too few testable signals show a "evidence too thin to verdict" state, not a forced verdict.
- **State persistence:** Phantom verdicts persist across refresh; planner overrides persist; saved scenarios survive reload — desert configuration, override set, planner reason notes all reload identically; district scores recompute deterministically from persistent state.
- **Human-in-the-loop override:** `team.planner_overrides` writes; `force_real` / `force_phantom` with required reason; the override flips the verdict and visibly recomputes the district score on the next toggle.

### Ambition signal
Beyond Track 2's minimum workflow ("select region → aggregate → drill down → save scenario"), this proposal ships a **subtraction-driven rescoring engine** with six pluggable existence tests (including embedding-drift), two adversarial agents reconciled by a deterministic majority-with-veto adjudicator, **AI activation-gated to the ~3% of contested verdicts** (Defender corroboration via Foundation Model API + IE evidence rows), per-district incremental recompute via CDC, a third dataset (India Post) used in a load-bearing way, **three planner deliverables in one app** (revised allocation CSV, inspector worksheet PDF, CKAN-compatible scenario export to S3 + mock HMIS webhook), and a **Genie NL sidebar** for ad-hoc questions over Lakebase — combinations most teams will not bother with.

---

## 4. Fragility to Dataset Issues

### Dataset correlations this idea depends on
- Correlation: VF claimed PIN ↔ India Post PIN-and-coordinate directory
  - Depends on: clean dedup of post-office fan-out before joining on `pincode`; enough facilities have both a claimed postcode and lat/long so the reverse-lookup test returns a usable signal.
  - **Day-0 validation: 95.5% of facility PINs resolve to India Post; 96.6% of facilities have both PIN and lat/lon. Strong.**
  - Breaks if: lat/long missing for the demo region, or PIN maps to multiple districts. Mitigation: the test is *"PIN inconsistent"*, not *"PIN missing"* — missing data does not fail this test.
- Correlation: VF facility lat/long ↔ NFHS-5 district indicators (via spatial join)
  - Depends on: high enough share of facilities geocode into districts so the NFHS outcome consistency test has signal; point-in-polygon spatial join (per the brief's recommendation).
  - **Day-0 validation: 99.9% of facilities point-in-polygon to an ADM2 district, 0% multi-assigned. 93.8% of spatial districts exact-match an NFHS row. Strong.**
  - Breaks if: spatial join coverage low or fanned out; pivot the demo to one or two states with strong coverage. Mitigation: skip the NFHS-consistency test when the join is ambiguous (don't fail it falsely).
- Correlation: VF structured-claim arrays (`capability` / `procedure` / `equipment`) ↔ MinHash near-duplicate fingerprints
  - Depends on: the structured-claim fields being populated and rich enough that MinHash signatures are meaningful.
  - Day-0 validation: 99.7% population rate on each; content is dense and unambiguous (procedures, equipment lists, capability statements). Exact-match dedup alone already groups 250 facilities into 92 near-identical clusters — non-trivial baseline before MinHash even runs.
  - **Note: original draft used `description` as the uniqueness signal — abandoned.** Description p50 = 16 tokens; Jaccard 0.9 + shingle 5 won't survive. The structured-claim arrays are 10–20× richer per facility.
  - Breaks if: structured claims dominated by short template phrases ("OPD services", "24x7 Emergency"), or legitimate template text from chain hospitals (Apollo, Fortis, Manipal) produces false positives. Day-0 false-positive check on Apollo (n=48): only 2 facilities (4%) duplicate-flagged — well under 30% threshold. Mitigation: tune Jaccard threshold offline; document trade-off in the verdict receipt.

### External dependencies
- **Facility coordinates required?** ☑ Yes ☐ Only as fallback ☐ No — load-bearing for spatial join and PIN reverse-lookup test (reduced demo region if necessary).
- **PIN→district join required?** ☑ Yes ☐ No — used as a load-bearing verification test, not just enrichment; via spatial join with PIN directory as the geocoding source.
- **NFHS-5 dependency?** ☑ Required ☐ Optional/contextual ☐ None — outcome consistency is one of the three core tests; story degrades materially without it.
- **Free-text quality dependency?** ☐ High (semantic parsing) ☐ Medium ☑ Low — MinHash on descriptions for the uniqueness test; very low text quality degrades the duplicate-detection signal.

### Worst-case demo failure mode
On a sparse district, all five existence tests return "indeterminate" → Adjudicator verdict is "contested" → district sees no phantom subtraction and the desert score is unchanged. **Graceful caveat:** side panel shows "0 phantoms detected — too little geographic data to test (district sparsity flag)" rather than a misleading "0 phantoms = real coverage" verdict. Users see *why* the district is data-poor — itself a planning insight. Misleading-result risk: high MinHash false-positive rate could subtract real facilities (e.g. franchised clinic chains using template `capability` arrays) — mitigated by treating MinHash alone as supporting evidence, never as the veto. Day-0 chain audit: 4% on Apollo (n=48).

### Day-0 audit outcomes (resolved)
*All mitigations below were either triggered or pre-emptively applied based on the 2026-06-15 validation run. Recorded here for the record.*
- ✅ **Demo state locked: Maharashtra.** Bihar fails T3.2 (24 single-flag facilities, 15 districts). Maharashtra: 304 phantom candidates, 33 districts, BEED rank shift 10→2.
- ✅ **NFHS-5 consistency test re-spec'd.** Original "indicator didn't move" requires NFHS-6 longitudinal comparison and is out of scope. Replacement: "facility claims capability X in a district whose NFHS-5 indicator for X sits in the bottom quartile of the state." Snapshot inconsistency, same story, implementable today.
- ✅ **Uniqueness test moved off `description` onto `capability`+`procedure`+`equipment`.** Description p50 = 16 tokens — too short for Jaccard 0.9 + shingle 5. Structured-claim arrays are 99.7% populated and far richer per facility.
- ✅ **`address_stateOrRegion` excluded from the input contract.** 254 distinct values vs. 36 real states; contains JSON fragments, city names, multi-state strings. State is derived from coordinates via the ADM2 spatial join.
- ✅ **`source_content_id` re-classified as page-level, not facility-level.** 27 different hospitals can share one scid. Used only as a citation back-reference. `phantom_verdicts` keys on `unique_id` (with the 11 row-twin duplicates dropped via `SELECT DISTINCT` at ingest).
- ✅ **Spatial join data sourced.** geoBoundaries India ADM2 (735 polygons, 48 MB GeoJSON) downloaded via GitHub LFS media endpoint — geoboundaries.org direct hits sandbox firewall.

### Open mitigations (not yet triggered)
- If MinHash false-positive rate on `capability` arrays turns out higher than 4% chain baseline once tuned: raise Jaccard threshold and/or require 5+ near-duplicate matches; document visibly in the verdict receipt.

### Robustness verdict
☐ Fragile — relies on sparse fields; missing data → broken UX
☑ **Mixed** — has a plan for sparsity but bleeds at edges (e.g., geographic joins)
☐ Robust — depends on highly-populated fields; sparsity → graceful degradation
☐ Anti-fragile — sparsity *is* the input; missing data increases value

**Justification:** The five-test architecture tolerates per-facility sparsity (any test can abstain) and the toggle-and-redraw mechanic degrades cleanly when test signals are missing (verdict = contested, no subtraction); but the *spatial joins* — central to two of the most visceral tests — bleed at the edges where coordinates and PINs disagree, and the demo's dramatic redraw moment requires enough phantoms in the demo region to be visible, which requires a deliberate state choice and coverage check on day one.

---

## 5. Differentiation vs Field Convergence

### Why this section exists
Hackathon teams using the same dataset and rubric will independently converge on the same generic Track 2 architecture: choropleth + LLM trust scores + override panel + saved scenarios. To stand out, Phantom Census deliberately absorbs the strongest mechanics from the team's other proposal candidates and grafts them onto the phantom-subtraction spine. Each addition is scope-managed (MVP-neutral or stretch-only) so the green-light rubric score doesn't degrade.

### Cross-pollinated mechanisms

| # | Mechanism | Source proposal | Why it escapes the generic prompt | Effort | MVP / Stretch |
|---|---|---|---|---|---|
| 1 | **Bitemporal time-slider replay** — `(valid_from, valid_to)` on existence tests + desert scores; rewind the choropleth to any past date and prove the system would have flagged Nalanda 9 months before the official survey. | Counterfactual Witness | Most teams treat trust as a static current-state score. Bitemporal turns it into an audit instrument that produces falsifiable hindsight receipts against public events. Demo beat: drag the slider, watch the past redraw. | +10–14h | Stretch |
| 2 | **Gold-set live model card** — 80 hand-labeled facilities (confirmed phantom / confirmed real / contested); live precision/recall/F1 panel updates as the planner overrides; cited rows clickable. | TrustLens, ClaimsCleanr | Generic implementations claim accuracy in abstract or hide it in test files. A live model card on stage demonstrates auditability *during* the demo, not just after. | +3–5h | Stretch (high ROI for credibility) |
| 3 | **Cross-track cascade preview** — after a phantom subtraction, a side overlay animates ASHA referral corridors re-routing through verified facilities (with new distances flagged). One Track 2 demo proves Track 3 value. | Regional Cascade Failure Simulator, Care Pathway Stress Test | No other Track 2 team will spend the engineering to embed Track 3 logic. The visual cascade ("12 maternity corridors re-route in 2 seconds") is unique and showy. | +8–10h | Stretch (highest demo wow) |
| 4 | **Silent-degradation 6th existence test** — `cache.description_embeddings` BYTEA + pgvector cosine index; flag facilities whose description drifted from substantive to template (or vice versa) between snapshots. | Provenance Pact | MinHash detects near-duplicates *now*. Embedding-drift detects facilities silently *becoming* phantoms (a real fraud pattern: stuff a real description, harvest the empanelment, blank it). Adds a temporal phantom-emergence signal absent in the 5-test core. Promoted to MVP per the AI-centrality patch — gives the Existence engine a non-MinHash semantic signal and is the storage substrate Vector Search reads. | +5–7h | **MVP** (promoted from stretch) |
| 5 | **Leverage-weighted phantom queue** — sort the override panel by `mortality_burden_percentile × population_proxy × phantom_density`, not alphabetical; highest-leverage phantoms surface first so planner time goes where it matters. | Public Health Leverage Clean Desk, Readiness Cascade | Tiny code change (ORDER BY) but huge operational signal. Generic implementations sort by detection time or facility ID. | +0.5–1h | **MVP** (scope-neutral upgrade) |
| 6 | **CDC cascade animation** — when an override fires, animate the event ripple: facility tile flips → district score recomputes → state ranking shuffles → side panel updates, all on one CDC stream subscription. | Recantation Ledger | Static recompute is the default. Watching causality propagate through Lakebase CDC in real time is itself the proof that the architecture is event-driven. | +2–3h | Stretch (cheap, high signal for technical depth) |

### What stays cut
- Multi-Agent Supervisor for adjudication: explicitly *not* added. Phantom Census already wins on deterministic adjudication; layering MAS on the verdict step would dilute the "no LLM in the math" message and tank the Tokens score.
- Twilio / SMS / actual external HMIS endpoint: out of MVP scope; adds integration risk without judging-criteria payoff over the existing S3 + mock webhook combo.
- Bitemporal write-replay of corrections (the "Reconciler" from Counterfactual Witness): kept as read-only time slider; the full diff-engine is out of MVP and out of stretch.

### Recommended scope-managed pick (if any stretch ships)
With embedding-drift now in MVP, remaining stretches are bitemporal slider, cross-track ASHA cascade, gold-set live model card, and CDC cascade animation. Pick **one** of {bitemporal slider, cross-track cascade} — either delivers a unique demo beat no other team will reproduce. The gold-set model card and CDC cascade animation are both cheap; ship them regardless. The **two MVP additions from the AI-centrality patch** (embedding-drift, Foundation Model API for contested cases) are NOT candidates for cutting — they are the answer to *"where's the AI?"*

### Cut path if hours run short (in order)
1. **Drop ArcLayer / cross-track cascade** — saves 8–10h; the toggle + ghosts is still the centerpiece.
2. **Drop bitemporal slider** — saves 10–14h.
3. **Drop CDC cascade animation** — saves 2–3h.
4. **Demote Audit Queue PDF export to CSV** — saves 1h, keeps the deliverable.
**Do NOT cut:** ghost overlay (free), Budget Reallocation view (the user-value answer), Genie sidebar (the cheap LLM win), embedding-drift (the AI-centrality answer), FMA on contested (same).

### Field-convergence sanity check
If a judge has watched 11 Track 2 demos before this one, what will look novel?
- **Subtract instead of add** (the core thesis — already differentiated).
- **Six deterministic existence tests run as Lakebase rows, not code** (already differentiated).
- **Ghosts stay visible after subtraction** — most teams will hide phantoms; we keep the lie on screen.
- **AI activation-gated to ~3% of records** with a visible *"determinism owns the math; AI owns the evidence; human owns the decision"* posture (cross-pollinated AI-centrality — addresses the *"where's the AI?"* misread without breaking the deterministic-core claim).
- **Three planner deliverables in one app** (revised allocation CSV + inspector worksheet PDF + CKAN scenario export) — most teams will ship one export and call it done.
- **Genie sidebar over Lakebase** — most teams won't expose ad-hoc NL over their own warehouse.
- **Bitemporal rewind producing falsifiable audit receipts** *(if stretch ships)* (cross-pollinated — would be unprecedented in Track 2).
- **Track 3 corridors re-routing inside a Track 2 app** *(if stretch ships)* (cross-pollinated — most teams don't even cross-cite tracks).
- **Live model card on the choropleth dashboard** *(if stretch ships)* (cross-pollinated — moves accuracy from claim to artifact).
