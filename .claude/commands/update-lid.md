---
name: update-lid
description: Reconcile LID configuration, refresh conventions, change mode (Full ↔ Scoped), or update scope globs. Use when LID drift is suspected or project structure has changed.
---

Invoke the `update-lid` skill. Reconciles drift between `AGENTS.md` / `CLAUDE.md` directives and the actual `docs/` tree, refreshes conventions, or transitions between LID modes. See `.claude/skills/update-lid/SKILL.md` for the full behavior.
